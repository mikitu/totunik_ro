{
  "collectionName": "components_contact_direction_items",
  "info": {
    "displayName": "Direction Item",
    "description": "Individual direction or transportation method"
  },
  "options": {},
  "attributes": {
    "type": {
      "type": "enumeration",
      "enum": ["metro", "car", "bus", "walking", "taxi"],
      "required": true,
      "default": "metro"
    },
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "string",
      "required": true
    },
    "icon": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "color": {
      "type": "enumeration",
      "enum": ["blue", "green", "yellow", "red", "purple", "orange"],
      "required": false,
      "default": "blue"
    }
  }
}
